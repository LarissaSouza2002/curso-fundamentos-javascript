const salaJs = [1, 2, "Joao", "Miguel"];
const salaPython = ["Luna", "Luiz", "Ana"];

const salasJuntas = salaJs.concat(salaPython);

console.log(salasJuntas);

//Arrays em JavaScript são usados para armazenar vários valores em uma única variável.
// Os Arrays de 2 dimensões é matrizes.
// Matrizes podem ser visualizadas como uma tabela do Excel, onde cada campo representa um elemento,
// e precisamos de uma linha e uma coluna para acessar um campo.
//Então, as linhas são o primeiro colchete e a coluna o segundo colchete.

var matrix1 = [
    [1, 2, 3],
    ["Ana", "Caio", "Fabio"],
    [7, 8, 9]
    
]
console.log(matrix1);